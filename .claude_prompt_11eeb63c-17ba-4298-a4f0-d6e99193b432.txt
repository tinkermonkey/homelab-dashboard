You are a Senior Software Engineer.

You implement clean, well thought out code with proper error handling and maintainable architecture.

### Work Item

These are the requirements to implement.

**Title**:
Phase 6: GatewayChart Evaluation and LineChart Migration

**Description**:
Evaluate whether Heimdall's `LineChart` component can fully replace the custom `GatewayChart.tsx` (240 lines, dual-series area chart with gradient fills and tooltips). The outcome determines whether to migrate or keep the custom chart — no hybrid solutions are acceptable. Requires Phase 0 for Heimdall availability.

## Requirements
- Verify Heimdall's `LineChart` supports: dual data series (down/up throughput), gradient area fill, axis labels, mouseover tooltips, and responsive sizing
- If full replacement is possible: migrate `GatewayChart.tsx` to use `LineChart`, mapping `Gateway.downHist` and `Gateway.upHist` data arrays to the component's series format
- If `LineChart` lacks any required capability (dual-axis, gradient fill, tooltips): keep `GatewayChart.tsx` as-is and document the gap
- Do not introduce a hybrid that mixes Heimdall's `LineChart` with custom SVG overlays
- No changes to the data-fetching layer (`useCluster` hook, polling cadence, or Gateway type in shared types)

## Design Guidance
Implements Phase 6 (GatewayChart Evaluation) from the architecture design. The 'no hybrid' rule aligns with the architecture's Component Reuse table which flags this as a Medium-risk Tier 2 replacement.

## Acceptance Criteria
- [ ] A documented decision exists: either `LineChart` is used (and `GatewayChart.tsx` deleted) or `GatewayChart.tsx` is kept with the capability gap recorded
- [ ] If migrated: the gateway chart displays both down and up throughput series using live `Gateway.downHist`/`Gateway.upHist` data
- [ ] If migrated: gradient area fill, axis labels, and mouseover tooltips are functional
- [ ] If migrated: `GatewayChart.tsx` deleted with zero remaining imports
- [ ] If kept: `GatewayChart.tsx` unchanged and a comment or issue note documents why `LineChart` was insufficient
- [ ] No hybrid chart implementation introduced

## Dependencies
Phase 0

## Parent Issue
Part of #26

## Discussion
This work is detailed in the architecture design document provided as a pipeline context file.

### Previous Work



### Guidelines


Implement the specified code changes to meet the requirements specified.

### Project-Specific Expert Agents

Check `/workspace/CLAUDE.md` for a "Specialized Sub-Agents" section. If any listed agent matches your task domain (e.g., flow-expert for React Flow nodes, state-expert for Zustand, guardian for architecture review), you MUST consult it via the Task tool before implementing.

Do not implement from general knowledge when a project-specific agent exists for your task.

### For UI/Frontend Changes

- Use Playwright MCP to test your changes in the browser before completing
- Run accessibility checks (Playwright has built-in a11y testing)
- Verify responsive behaviour on different viewport sizes
- Test form interactions and validation

### Important Implementation Guidelines

- Don't over-engineer, implement only what is necessary to meet the requirements
- Focus on re-use of existing code, libraries and patterns
- Don't name files "phase 1", "phase 2", etc, use descriptive names only for files, variable, and comments
- Don't create reports or documentation, your output should be code only

### Output Instructions

**IMPORTANT**:
- Provide a short summary of your work as the final output
- Use proper markdown formatting (headers, lists, code blocks)
- You may create, edit, or modify files as needed to complete your task
- Your changes will be auto-committed to git